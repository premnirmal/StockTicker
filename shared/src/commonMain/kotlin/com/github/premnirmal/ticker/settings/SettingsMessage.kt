package com.github.premnirmal.ticker.settings

/**
 * Platform-neutral user-facing message keys emitted by [SettingsViewModel]. The Android UI resolves
 * each key to a localized string resource, keeping the resource lookup in `:app`.
 */
enum class SettingsMessage {
    DaysUpdatedError,
}
