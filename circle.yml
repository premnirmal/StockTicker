machine:
  java:
    version: oraclejdk8
  environment:
    GRADLE_OPTS: '-Dorg.gradle.jvmargs="-Xmx2048m -XX:MaxPermSize=1024m -Xms512m -XX:+HeapDumpOnOutOfMemoryError"'
dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "android-25"
    - echo y | android update sdk --no-ui --all --filter "platform-tools"
    - echo y | android update sdk --no-ui --all --filter "build-tools-25.0.2"
    - echo y | android update sdk --no-ui --all --filter "extra-android-m2repository"
    - echo y | android update sdk --no-ui --all --filter "extra-android-support"
    - echo y | android update sdk --no-ui --all --filter "extra-google-m2repository"
test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) ./gradlew assemblePureflossDebug -PdisablePreDex ;; 1) ./gradlew testPureflossDebugUnitTest -PdisablePreDex --no-daemon ;; esac:
        parallel: true
    - cp -r app/build/outputs $CIRCLE_ARTIFACTS
